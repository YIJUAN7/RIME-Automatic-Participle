# 三码/四码输入法自动切分逻辑

## 设计目标

在只有三码字和四码字的码表体系下，实现连续输入时尽可能少按空格键。

## 核心思想

**判断首字是三码字还是四码字**，通过检查后续编码是否能成字来消歧。

- 若后续编码能组成有效的三码字或四码字 → 首字可能是三码字
- 若后续编码既不能组成三码字也不能组成四码字 → 首字只能是四码字（或反之）

## 切分规则

### 4码时

| 条件 | 判断 | 操作 |
|------|------|------|
| 三码成字 + 四码不成字 | 首字一定是三码字 | 上屏三码字 |
| 三码不成字 + 四码成字 | 首字可能是四码字 | 等待第5码确认 |
| 都不成字 | 输入错误 | 清屏 |
| 都成字 | 无法判断 | 等待用户继续输入或手动选字 |

### 5码时

| 条件 | 判断 | 操作 |
|------|------|------|
| 三码不成字 + 四码成字 | 首字一定是四码字 | 上屏四码字 |
| 三码成字 + 四码成字 | 无法判断 | 等待继续输入 |

### 7码时（首字 + 第二字前缀）

可能组合：`3+4`、`4+3`、`3+3+n`

| 条件 | 判断 | 操作 |
|------|------|------|
| 末4码不成字 + 末3码不成字 | `3+4`和`3+3`都不成立，首字只能是四码字 | 上屏前四码字 |
| 其他 | 无法判断 | 等待第8码 |

### 8码时（首字 + 第二字 + 第三字前缀）

可能组合：`3+3+n`、`3+4+n`、`4+3+n`、`4+4`

| 条件 | 判断 | 操作 |
|------|------|------|
| 末4码不成字 + 末3码不成字 | `4+3+n`和`4+4`都不成立，首字只能是三码字 | 上屏前三码字 |
| 其他 | 无法判断 | 等待用户手动选字或继续输入 |

## 消歧原理

设首字编码长度为 `L`（3或4），剩余编码为 `R`：

1. 若 `L=3`，则 `R` 的前几码应能组成有效的字
2. 若 `L=4`，则 `R` 的前几码也应能组成有效的字

当 `R` 的前几码既不能组成三码字也不能组成四码字时，说明某种分割方式不成立，从而确定首字的长度。

## 实现文件

- `lua/auto_commit.lua` - Processor，实现自动上屏逻辑
- `lua/my_translate.lua` - Translator，长编码时显示候选供手动选择
- `utf8_sub.lua` - 依赖文件,提供utf8字符串切分功能